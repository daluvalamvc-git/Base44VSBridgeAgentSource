<Window x:Class="Base44AIPilot.UI.DiffPreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Base44 AI — Review Changes"
        Width="820" Height="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Header -->
    <Border Grid.Row="0" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
            Padding="16,10"
            BorderThickness="0,0,0,1"
            BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}">
      <StackPanel Orientation="Vertical">
        <TextBlock Text="Review AI-Suggested Changes"
                   FontSize="14" FontWeight="SemiBold"
                   Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"/>
        <TextBlock Text="Review each change below. Click Apply to write a file to disk."
                   FontSize="11" Opacity="0.7"
                   Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"/>
      </StackPanel>
    </Border>

    <!-- Change list -->
    <ScrollViewer Grid.Row="1"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="16,12">
      <StackPanel x:Name="ChangeListPanel" Orientation="Vertical"/>
    </ScrollViewer>

    <!-- Footer buttons -->
    <Border Grid.Row="2"
            BorderThickness="0,1,0,0"
            BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
            Padding="16,10"
            Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="Apply All New Files"
                Click="ApplyAllButton_Click"
                Padding="14,6" Margin="0,0,8,0"
                Background="#388E3C" Foreground="White"
                BorderThickness="0" FontWeight="SemiBold"/>
        <Button Content="Close"
                Click="CloseButton_Click"
                Padding="14,6"
                IsCancel="True"/>
      </StackPanel>
    </Border>
  </Grid>
</Window>
